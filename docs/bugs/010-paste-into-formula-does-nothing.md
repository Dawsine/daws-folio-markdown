# 010　复制 `$C_{V_n}$` 后文档里粘不上

**状态：** 0.1.16 光标先离开公式预览再插入  
**报告人：** 用户（2026-09-15）

## 现象

复制空位谱 `$C_{V_n}$`，在同一篇 Folio 文档里 Cmd+V 没有字进去。

## 原因

公式预览是 `contenteditable=false`。复制后选区还在 KaTeX 里。0.1.15 的粘贴拦了系统默认，又往不可编辑节点 `insertNode`，等于吃掉这一下。

## 处理

粘贴前把光标挪到公式块后面；没有可用选区就落到正文末尾。复制下来的 `$...$` 仍按公式插入。
