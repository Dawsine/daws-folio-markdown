# daws-folio-markdown

Cursor/VS Code 扩展。只做 Markdown 所见即所得，对齐 Office Viewer 的 Markdown 能力，表格行内公式必须能自己修。

先读 `docs/CONTRACT.md`。不要复制 Office Viewer 源码。公式进出编辑器必须走 `src/tableMath.ts`。

用户向中文跟用户自己的句子：短、说自己碰到的事，不写说明书和产品腔。英文只翻译中文那点信息，不另加修辞。
